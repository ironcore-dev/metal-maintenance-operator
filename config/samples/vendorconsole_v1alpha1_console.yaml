apiVersion: vendorconsole.metal.ironcore.dev/v1alpha1
kind: Console
metadata:
  labels:
    app.kubernetes.io/name: metal-maintenance-operator
    app.kubernetes.io/managed-by: kustomize
  name: console-sample
spec:
  # TODO(user): Add fields here
  connection:
    url: https://openmanage.example.com
    # insecureSkipTLSVerify disables TLS certificate verification for consoles
    # presenting self-signed or otherwise untrusted certificates.
    insecureSkipTLSVerify: false
