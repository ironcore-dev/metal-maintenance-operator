---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: ansiblejobs.maintenance.metal.ironcore.dev
spec:
  group: maintenance.metal.ironcore.dev
  names:
    kind: AnsibleJob
    listKind: AnsibleJobList
    plural: ansiblejobs
    singular: ansiblejob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.playbook
      name: Playbook
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AnsibleJob is the Schema for the ansiblejobs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AnsibleJobSpec defines the desired state of AnsibleJob
            properties:
              extraVars:
                description: ExtraVars are additional variables to pass to the playbook
                items:
                  description: KeyValue represents a key-value pair for extra variables
                  properties:
                    name:
                      description: Name is the variable name
                      type: string
                    value:
                      description: Value is the variable value
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              inventory:
                description: Inventory defines the target hosts
                properties:
                  configMapRef:
                    description: ConfigMapRef references a ConfigMap containing the
                      inventory
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  inline:
                    description: Inline inventory as YAML/JSON string
                    type: string
                  secretRef:
                    description: SecretRef references a Secret containing the inventory
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
              jobTemplate:
                description: JobTemplate allows customization of the Kubernetes Job
                properties:
                  backoffLimit:
                    description: BackoffLimit specifies the number of retries before
                      marking this job failed
                    format: int32
                    type: integer
                  image:
                    description: Image is the container image to use for ansible-runner
                    type: string
                  resources:
                    description: Resources specifies the compute resource requirements
                    properties:
                      limits:
                        description: Limits describes the maximum amount of compute
                          resources allowed
                        items:
                          description: ResourceQuantity represents a named resource
                            quantity
                          properties:
                            name:
                              description: Name is the resource name (e.g., "cpu",
                                "memory")
                              type: string
                            quantity:
                              description: Quantity is the resource quantity (e.g.,
                                "100m", "512Mi")
                              type: string
                          required:
                          - name
                          - quantity
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      requests:
                        description: Requests describes the minimum amount of compute
                          resources required
                        items:
                          description: ResourceQuantity represents a named resource
                            quantity
                          properties:
                            name:
                              description: Name is the resource name (e.g., "cpu",
                                "memory")
                              type: string
                            quantity:
                              description: Quantity is the resource quantity (e.g.,
                                "100m", "512Mi")
                              type: string
                          required:
                          - name
                          - quantity
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                    type: object
                  serviceAccountName:
                    description: ServiceAccountName for the Job
                    type: string
                type: object
              limit:
                description: Limit restricts the playbook run to specific hosts
                type: string
              playbook:
                description: Playbook specifies the playbook to run
                type: string
              playbookGitRef:
                description: PlaybookGitRef specifies the branch, tag, or commit to
                  use for the playbook repository
                type: string
              playbookRepo:
                description: PlaybookRepo is the repository containing playbooks
                type: string
              rolesGitRef:
                description: RolesGitRef specifies the branch, tag, or commit to use
                  for the roles repository
                type: string
              rolesRepo:
                description: RolesRepo is the repository containing roles
                type: string
              timeoutSeconds:
                description: TimeoutSeconds specifies the job timeout
                format: int32
                type: integer
            required:
            - inventory
            - playbook
            - playbookRepo
            type: object
          status:
            description: AnsibleJobStatus defines the observed state of AnsibleJob
            properties:
              completionTime:
                description: CompletionTime is when the job completed
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              jobId:
                description: JobID is the ansible-runner job ID
                type: string
              jobName:
                description: JobName is the name of the created Kubernetes Job
                type: string
              message:
                description: Message provides additional details about the job status
                type: string
              phase:
                description: Phase represents the current phase of the job
                type: string
              results:
                description: Results contains the results from the ansible execution
                properties:
                  artifactsPath:
                    description: ArtifactsPath is the path to the artifacts from the
                      run
                    type: string
                  exitCode:
                    description: ExitCode from the ansible-runner execution
                    format: int32
                    type: integer
                  stats:
                    description: Stats contains the execution statistics as JSON string
                    type: string
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              startTime:
                description: StartTime is when the job started
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
