---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: firmwareupdatehpes.system.metal.ironcore.dev
spec:
  group: system.metal.ironcore.dev
  names:
    kind: FirmwareUpdateHPE
    listKind: FirmwareUpdateHPEList
    plural: firmwareupdatehpes
    shortNames:
    - fwuh
    singular: firmwareupdatehpe
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .spec.serverRef.name
      name: ServerRef
      type: string
    - jsonPath: .spec.serverMaintenanceRef.name
      name: ServerMaintenanceRef
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FirmwareUpdateHPE is the Schema for the firmwareupdatehpes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FirmwareUpdateHPESpec defines the desired state of FirmwareUpdateHPE.
            properties:
              retryPolicy:
                description: RetryPolicy defines the retry behavior for automatic
                  retries on transient failures.
                properties:
                  maxAttempts:
                    description: |-
                      MaxAttempts is the maximum number of automatic retry attempts after failure.
                      0 means no automatic retries. Must be between 0 and 10 inclusive.
                      If not set, the operator-level default is used.
                    format: int32
                    maximum: 10
                    minimum: 0
                    type: integer
                type: object
              serverMaintenancePolicy:
                description: |-
                  ServerMaintenancePolicy is a maintenance policy to be enforced on the server
                  (OwnerApproval | Enforced).
                type: string
              serverMaintenanceRef:
                description: |-
                  ServerMaintenanceRef is a reference to a ServerMaintenance object that the controller has
                  requested for the referred server. Reboot safety is delegated entirely to this
                  ServerMaintenance (the iLO Install Set reboots the host and there is no client apply-time
                  control) — the apply pass is gated until the Server is parked for maintenance, exactly as
                  the Dell and Lenovo controllers do.
                properties:
                  name:
                    description: Name is the name of the referenced object.
                    type: string
                  namespace:
                    description: Namespace is the namespace of the referenced object.
                    type: string
                required:
                - name
                - namespace
                type: object
              serverRef:
                description: ServerRef is a reference to a specific server to apply
                  the SPP-based firmware update on.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: serverRef is immutable
                  rule: self == oldSelf
              spp:
                description: SPP describes the HPE Service Pack for ProLiant the controller
                  diffs and applies.
                properties:
                  baseURI:
                    description: |-
                      BaseURI is the HTTP(S) base URL where the extracted SPP is served (reachable by iLO). The
                      controller reads `<BaseURI>/manifest/metadata.json` for the catalog, and hands iLO
                      `<BaseURI>/packages/<file>.fwpkg` URIs via AddFromUri to stage components.
                    type: string
                  secretRef:
                    description: SecretRef references credentials for the SPP server,
                      if it requires authentication.
                    properties:
                      name:
                        description: name is unique within a namespace to reference
                          a secret resource.
                        type: string
                      namespace:
                        description: namespace defines the space within which the
                          secret name must be unique.
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - baseURI
                type: object
            required:
            - serverRef
            - spp
            type: object
          status:
            description: FirmwareUpdateHPEStatus defines the observed state of FirmwareUpdateHPE.
            properties:
              conditions:
                description: |-
                  Conditions represents the latest available observations of the SPP-based firmware update
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failedAttempts:
                description: FailedAttempts is the number of automatic retry attempts
                  made after failure.
                format: int32
                type: integer
              installSetID:
                description: InstallSetID is the id of the iLO Install Set created
                  for the current pass.
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              passCount:
                description: |-
                  PassCount is the number of check->apply->recheck passes completed so far. It bounds the
                  internal convergence loop.
                format: int32
                type: integer
              state:
                description: State represents the current state of the SPP-based firmware
                  update.
                type: string
              updateComponents:
                description: |-
                  UpdateComponents is the applicable update set computed by the dry-run diff (and tracked
                  through apply).
                items:
                  description: |-
                    ComponentUpdate is one component the diff selected for update: the device it targets, the
                    versions involved, and the payload staged into the iLO ComponentRepository.
                  properties:
                    availableVersion:
                      description: AvailableVersion is the version offered by the
                        SPP manifest.
                      type: string
                    deviceName:
                      description: DeviceName is the human-readable device name from
                        the SPP manifest.
                      type: string
                    filename:
                      description: |-
                        Filename is the payload staged into the iLO ComponentRepository and referenced by the
                        Install Set Sequence. Resolved from the SPP manifest's `Package.Files[].Name` (the entry
                        whose TargetGUIDs contains Target) — not `FirmwareImages[].FileName`.
                      type: string
                    installedVersion:
                      description: InstalledVersion is the version currently reported
                        by FirmwareInventory.
                      type: string
                    state:
                      description: State is the per-component state as reported by
                        the iLO UpdateTaskQueue.
                      type: string
                    target:
                      description: |-
                        Target is the HPE device Target GUID — the join key between the SPP manifest, the iLO
                        FirmwareInventory, and the ComponentRepository.
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
