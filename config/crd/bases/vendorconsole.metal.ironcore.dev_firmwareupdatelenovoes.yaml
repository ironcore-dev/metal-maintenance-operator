---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: firmwareupdatelenovoes.vendorconsole.metal.ironcore.dev
spec:
  group: vendorconsole.metal.ironcore.dev
  names:
    kind: FirmwareUpdateLenovo
    listKind: FirmwareUpdateLenovoList
    plural: firmwareupdatelenovoes
    singular: firmwareupdatelenovo
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.lxcaURL
      name: lxcaURL
      type: string
    - jsonPath: .status.serverCount
      name: serverCount
      type: integer
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FirmwareUpdateLenovo is the Schema for the firmwareupdatelenovoes
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of FirmwareUpdateLenovo
            properties:
              compliancePolicy:
                description: |-
                  CompliancePolicy describes the LXCA compliance policy the controller
                  should create (or reuse) for this update.
                properties:
                  description:
                    description: Description is set on the LXCA policy when the controller
                      creates one.
                    type: string
                  name:
                    description: |-
                      Name is the LXCA compliance policy name. If a policy with this name
                      already exists on the LXCA appliance the controller reuses it.
                    type: string
                required:
                - name
                type: object
              firmwarePayload:
                description: |-
                  FirmwarePayload describes the firmware bundle LXCA should import into
                  its repository before applying updates.
                properties:
                  checksum:
                    description: |-
                      Checksum is an optional hash of the payload passed through to LXCA
                      where the endpoint supports validation.
                    type: string
                  sourceType:
                    description: SourceType controls which sub-field of this struct
                      is used.
                    enum:
                    - URL
                    - Upload
                    type: string
                  url:
                    description: |-
                      URL is the location of a UXSP or firmware package that LXCA can
                      download itself. Required when SourceType is "URL".
                    type: string
                required:
                - sourceType
                type: object
              lxcaURL:
                description: LXCAURL is the base URL of the target Lenovo XClarity
                  Administrator.
                pattern: ^https?://.+$
                type: string
              secretRef:
                description: |-
                  SecretRef points at a Secret in the same namespace as this CR that
                  holds LXCA credentials. The Secret must contain "username" and
                  "password" keys; the controller writes back "token" and "sessionID"
                  keys after login so subsequent reconciles can reuse the session.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              serverMaintenancePolicy:
                default: Enforced
                description: |-
                  ServerMaintenancePolicy is passed through to each ServerMaintenance
                  resource the controller creates while the flash is running. Defaults
                  to Enforced.
                type: string
              serverSelector:
                description: |-
                  ServerSelector picks the metalv1alpha1/v1alpha1 Server objects to
                  flash. Servers whose Status.Manufacturer is not "Lenovo" cause the
                  update to transition to Failed.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              updateAction:
                default: Immediate
                description: |-
                  UpdateAction maps to LXCA's activation parameter on
                  POST /updatableComponents. Defaults to Immediate.
                enum:
                - Immediate
                - Prioritized
                type: string
            required:
            - compliancePolicy
            - firmwarePayload
            - lxcaURL
            - secretRef
            - serverSelector
            type: object
          status:
            description: status defines the observed state of FirmwareUpdateLenovo
            properties:
              compliancePolicyID:
                description: |-
                  CompliancePolicyID is the ID LXCA assigned to the compliance policy
                  this controller created or reused.
                type: string
              conditions:
                description: |-
                  Conditions represent the current state of the FirmwareUpdateLenovo
                  resource. The controller maintains a single "UpdateCompleted" type.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              maintenanceRefs:
                description: |-
                  MaintenanceRefs tracks ServerMaintenance resources the controller
                  created; they are removed on completion or on CR deletion.
                items:
                  description: |-
                    MaintenanceRef records a ServerMaintenance resource this CR created so the
                    controller can clean it up on completion or deletion.
                  properties:
                    maintenanceName:
                      description: |-
                        MaintenanceName is the name of the ServerMaintenance the controller
                        created; it lives in the same namespace as this CR.
                      type: string
                    serverName:
                      description: ServerName is the name of the target metalv1alpha1.Server.
                      type: string
                  required:
                  - maintenanceName
                  - serverName
                  type: object
                type: array
              repositoryJobID:
                description: |-
                  RepositoryJobID is the LXCA task ID returned from
                  POST /files/updateRepositories/firmware/import while the payload is
                  being imported. It is cleared once the import completes.
                type: string
              serverCount:
                description: |-
                  ServerCount is the number of Servers matched by the selector at the
                  moment the update started.
                format: int32
                type: integer
              state:
                description: State is the top-level lifecycle state.
                type: string
              updateJobID:
                description: |-
                  UpdateJobID is the LXCA task ID returned from
                  POST /updatableComponents.
                type: string
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: firmwarePayload is immutable while status.state is InProgress
          rule: '!has(oldSelf.status) || !has(oldSelf.status.state) || oldSelf.status.state
            != ''InProgress'' || self.spec.firmwarePayload == oldSelf.spec.firmwarePayload'
    served: true
    storage: true
    subresources:
      status: {}
