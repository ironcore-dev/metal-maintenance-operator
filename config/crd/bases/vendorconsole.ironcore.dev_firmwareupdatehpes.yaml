---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: firmwareupdatehpes.vendorconsole.ironcore.dev
spec:
  group: vendorconsole.ironcore.dev
  names:
    kind: FirmwareUpdateHPE
    listKind: FirmwareUpdateHPEList
    plural: firmwareupdatehpes
    singular: firmwareupdatehpe
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FirmwareUpdateHPE is the Schema for the firmwareupdatehpes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FirmwareUpdateHPESpec defines the desired state of FirmwareUpdateHPE.
            properties:
              firmwareBundle:
                description: |-
                  FirmwareBundle is the identifier of the firmware image to be used for the update.
                  find the name and version of the firmware bundle in HPE OneView UI.
                  kubebuilder:validation:Required
                properties:
                  name:
                    description: |-
                      Name is the name of the firmware bundle.
                      if provided along with Version, it helps to uniquely identify the firmware bundle.
                    type: string
                  uuid:
                    description: |-
                      UUID is the UUID of the firmware bundle.
                      this is optional, if provided, it helps to uniquely identify the firmware bundle.
                    type: string
                  version:
                    description: |-
                      Version is the version of the firmware bundle.
                      if provided along with name, it helps to uniquely identify the firmware bundle.
                    type: string
                type: object
              firmwareBundleUpdateOptions:
                description: |-
                  FirmwareBundleUpdateOptions defines how the firmware bundle is to be applied to the server.
                  Possible values are 'ServerProfileTemplate' and 'ServerProfile'.
                  kubebuilder:validation:CEL:rule="!has(oldSelf.status) || !has(oldSelf.status.state) || oldSelf.status.state != 'InProgress' || self == oldSelf",message="spec is immutable when firmware update is in progress"
                  kubebuilder:validation:Enum=ServerProfileTemplate;ServerProfile
                type: string
              ovURL:
                description: OMEURL is the URL of the Dell OpenManage Enterprise (OME)
                  instance.
                pattern: ^https?://[a-zA-Z0-9.-]+(:[0-9]+)?(/.*)?$
                type: string
              secretRef:
                description: |-
                  secretRef is a reference to the Kubernetes Secret (of type SecretTypeBasicAuth) object that contains the credentials
                  to access the HPE OneView (OV). This secret includes sensitive information such as usernames and passwords.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              serverMaintenancePolicy:
                description: ServerMaintenancePolicy is a maintenance policy to be
                  enforced on the server managed by referred BMC.
                type: string
              serverMaintenanceRefs:
                description: ServerMaintenanceRefs are references to a ServerMaintenance
                  objects that Controller has requested for the each of the related
                  server.
                items:
                  description: ServerMaintenanceRefItem is a reference to a ServerMaintenance
                    object.
                  properties:
                    serverMaintenanceRef:
                      description: ServerMaintenanceRef is a reference to a ServerMaintenance
                        object that the BMCSettings has requested for the referred
                        server.
                      properties:
                        apiVersion:
                          description: API version of the referent.
                          type: string
                        fieldPath:
                          description: |-
                            If referring to a piece of an object instead of an entire object, this string
                            should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                            For example, if the object reference is to a container within a pod, this would take on a value like:
                            "spec.containers{name}" (where "name" refers to the name of the container that triggered
                            the event) or if no container name is specified "spec.containers[2]" (container with
                            index 2 in this pod). This syntax is chosen only to have some well-defined way of
                            referencing a part of an object.
                          type: string
                        kind:
                          description: |-
                            Kind of the referent.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                          type: string
                        name:
                          description: |-
                            Name of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        namespace:
                          description: |-
                            Namespace of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                          type: string
                        resourceVersion:
                          description: |-
                            Specific resourceVersion to which this reference is made, if any.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                          type: string
                        uid:
                          description: |-
                            UID of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                  type: object
                type: array
              serverSelector:
                description: ServerSelector specifies a label selector to identify
                  the servers that are to be selected.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - firmwareBundle
            - ovURL
            - secretRef
            - serverSelector
            type: object
          status:
            description: FirmwareUpdateHPEStatus defines the observed state of FirmwareUpdateHPE.
            properties:
              completedServerCount:
                description: CompletedServerCount is the total number of servers that
                  have completed the firmware update.
                format: int32
                type: integer
              conditions:
                description: Conditions represents the latest available observations
                  of the Bios version upgrade state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedServerCount:
                description: FailedServerCount is the total number of servers that
                  have failed the firmware update.
                format: int32
                type: integer
              inPendingServerCount:
                description: InPendingServerCount is the total number of servers that
                  are currently waiting of serverMaintenance to be approved.
                format: int32
                type: integer
              inProgressServerCount:
                description: InProgressServerCount is the total number of servers
                  that are currently in progress of firmware update.
                format: int32
                type: integer
              serverCount:
                description: ServerCount is the total number of servers selected by
                  the ServerSelector.
                format: int32
                type: integer
              state:
                description: State represents the current state of the bios configuration
                  task.
                type: string
              updateTask:
                description: UpdateTask contains the state of the Update Task created
                  by the OV for the firmware upgrade.
                items:
                  properties:
                    patchFirmwareBuncleStatus:
                      properties:
                        jobId:
                          description: Id is the unique identifier for the job created
                            in OME.
                          type: string
                        name:
                          description: Name is the name of the job.
                          type: string
                        status:
                          description: Status represents the current status of the
                            job.
                          type: string
                      type: object
                    serverName:
                      type: string
                    serverPowerTaskStatus:
                      properties:
                        jobId:
                          description: Id is the unique identifier for the job created
                            in OME.
                          type: string
                        name:
                          description: Name is the name of the job.
                          type: string
                        status:
                          description: Status represents the current status of the
                            job.
                          type: string
                      type: object
                    serverUUID:
                      type: string
                    updateTaskStatus:
                      properties:
                        jobId:
                          description: Id is the unique identifier for the job created
                            in OME.
                          type: string
                        name:
                          description: Name is the name of the job.
                          type: string
                        status:
                          description: Status represents the current status of the
                            job.
                          type: string
                      type: object
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
