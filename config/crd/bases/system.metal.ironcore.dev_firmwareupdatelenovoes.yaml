---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: firmwareupdatelenovoes.system.metal.ironcore.dev
spec:
  group: system.metal.ironcore.dev
  names:
    kind: FirmwareUpdateLenovo
    listKind: FirmwareUpdateLenovoList
    plural: firmwareupdatelenovoes
    shortNames:
    - fwul
    singular: firmwareupdatelenovo
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .spec.serverRef.name
      name: ServerRef
      type: string
    - jsonPath: .spec.serverMaintenanceRef.name
      name: ServerMaintenanceRef
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FirmwareUpdateLenovo is the Schema for the firmwareupdatelenovoes
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FirmwareUpdateLenovoSpec defines the desired state of FirmwareUpdateLenovo.
            properties:
              repository:
                description: Repository describes the firmware repository XCC self-orchestrates
                  against.
                properties:
                  groupRequest:
                    description: |-
                      GroupRequest maps to the UpdateFromRepository GroupRequest parameter (whether the request
                      originates from a group service). Defaults to false.
                    type: boolean
                  mountOptions:
                    description: |-
                      MountOptions are passed through to the XCC as the RepoMountOpt action parameter, used for
                      CIFS/NFS mounts (e.g. "vers=3.0").
                    type: string
                  repoURI:
                    description: |-
                      RepoURI is the repository server address XCC pulls the firmware bundle from. It is the only
                      mandatory input to UpdateFromRepository. Point it at the bundle directory (which holds the
                      root <bundle>_index.json catalog + per-component metadata + payloads/), not at a file, e.g.
                      "https://10.0.0.1/firmware/sr650v3". Supported transports: CIFS / NFS / HTTP / HTTPS / SFTP.

                      Note: CIFS/NFS/HTTPS are license-gated on XCC (Enterprise / Platinum / Premier by generation);
                      plain HTTP is not listed as gated. See the design doc referenced above.
                    type: string
                  secretRef:
                    description: |-
                      SecretRef references the credentials (username/password) used to authenticate against the
                      repository server, if required. Maps to the RepoUserName / RepoPassword action parameters.
                    properties:
                      name:
                        description: name is unique within a namespace to reference
                          a secret resource.
                        type: string
                      namespace:
                        description: namespace defines the space within which the
                          secret name must be unique.
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - repoURI
                type: object
              retryPolicy:
                description: RetryPolicy defines the retry behavior for automatic
                  retries on transient failures.
                properties:
                  maxAttempts:
                    description: |-
                      MaxAttempts is the maximum number of automatic retry attempts after failure.
                      0 means no automatic retries. Must be between 0 and 10 inclusive.
                      If not set, the operator-level default is used.
                    format: int32
                    maximum: 10
                    minimum: 0
                    type: integer
                type: object
              serverMaintenancePolicy:
                description: |-
                  ServerMaintenancePolicy is a maintenance policy to be enforced on the server
                  (OwnerApproval | Enforced).
                type: string
              serverMaintenanceRef:
                description: |-
                  ServerMaintenanceRef is a reference to a ServerMaintenance object that the controller has
                  requested for the referred server. Reboot safety is delegated entirely to this
                  ServerMaintenance (there is no reboot-control parameter on UpdateFromRepository) — the apply
                  pass is gated until the Server enters Maintenance state, exactly as the Dell FirmwareUpdateDell
                  controller does.
                properties:
                  name:
                    description: Name is the name of the referenced object.
                    type: string
                  namespace:
                    description: Namespace is the namespace of the referenced object.
                    type: string
                required:
                - name
                - namespace
                type: object
              serverRef:
                description: ServerRef is a reference to a specific server to apply
                  the repository-based firmware update on.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: serverRef is immutable
                  rule: self == oldSelf
            required:
            - repository
            - serverRef
            type: object
          status:
            description: FirmwareUpdateLenovoStatus defines the observed state of
              FirmwareUpdateLenovo.
            properties:
              checkTask:
                description: CheckTask contains the state of the dry-run GetRepoUpdateDetail
                  task.
                properties:
                  message:
                    description: Message is the XCC-reported status message.
                    type: string
                  name:
                    description: Name is the Task's display name.
                    type: string
                  percentComplete:
                    description: PercentComplete is the XCC-reported completion percentage.
                    format: int32
                    type: integer
                  state:
                    description: State is the XCC-reported raw Redfish TaskState string.
                    type: string
                  taskID:
                    description: TaskID is the Redfish Task identifier tracking the
                      repository operation.
                    type: string
                type: object
              conditions:
                description: |-
                  Conditions represents the latest available observations of the repository-based firmware
                  update state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failedAttempts:
                description: FailedAttempts is the number of automatic retry attempts
                  made after failure.
                format: int32
                type: integer
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              passCount:
                description: |-
                  PassCount is the number of check->apply->track->recheck passes completed so far. It bounds
                  the internal convergence loop.
                format: int32
                type: integer
              state:
                description: State represents the current state of the repository-based
                  firmware update.
                type: string
              updateTask:
                description: UpdateTask contains the state of the main UpdateFromRepository
                  apply task.
                properties:
                  message:
                    description: Message is the XCC-reported status message.
                    type: string
                  name:
                    description: Name is the Task's display name.
                    type: string
                  percentComplete:
                    description: PercentComplete is the XCC-reported completion percentage.
                    format: int32
                    type: integer
                  state:
                    description: State is the XCC-reported raw Redfish TaskState string.
                    type: string
                  taskID:
                    description: TaskID is the Redfish Task identifier tracking the
                      repository operation.
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
